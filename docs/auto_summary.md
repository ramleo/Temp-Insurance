# ML Pipeline Summary

_Generated by auto_pipeline.py on 2026-06-01 10:59:28_

---

## 1. Dataset Overview

| Property | Value |
|---|---|
| File | Insurance_1000.csv |
| Rows | 999 |
| Columns | 21 |
| Target Column | `Premium Amount` |
| Task Type | **Regression** |
| Unique Target Values | 745 |
| Deployment Platform | render |

### Missing Values
| column | missing | pct |
| --- | --- | --- |
| Age | 15.0 | 1.5 |
| Annual Income | 43.0 | 4.3 |
| Marital Status | 18.0 | 1.8 |
| Number of Dependents | 89.0 | 8.9 |
| Occupation | 319.0 | 31.9 |
| Health Score | 61.0 | 6.1 |
| Previous Claims | 313.0 | 31.3 |
| Credit Score | 124.0 | 12.4 |
| Customer Feedback | 67.0 | 6.7 |

---

## 2. Features Used

**Numeric (9):** `id`, `Age`, `Annual Income`, `Number of Dependents`, `Health Score`, `Previous Claims`, `Vehicle Age`, `Credit Score`, `Insurance Duration`

**Categorical (11):** `Gender`, `Marital Status`, `Education Level`, `Occupation`, `Location`, `Policy Type`, `Policy Start Date`, `Customer Feedback`, `Smoking Status`, `Exercise Frequency`, `Property Type`

**Dropped (>50% missing):** _none_

---

## 3. Preprocessing Pipeline

| Step | Detail |
|---|---|
| Missing (numeric) | SimpleImputer(strategy="median") |
| Missing (categorical) | SimpleImputer(strategy="most_frequent") |
| Scaling | RobustScaler |
| Encoding | OneHotEncoder(handle_unknown="ignore") |
| Train/Test Split | 80/20, random_state=42 |
| Train size | 799 rows |
| Test size | 200 rows |

---

## 4. Model Selection & Hyperparameter Tuning

GridSearchCV(cv=3, n_jobs=-1, scoring="r2")

| Model | CV Score | Best Params |
|---|---|---|
| XGBoost | -0.0025 | {'learning_rate': 0.05, 'max_depth': 4, 'n_estimators': 100} |
| GradientBoosting | -0.0104 | {'learning_rate': 0.05, 'n_estimators': 100} |
| RandomForest | -0.0107 | {'max_depth': 5, 'n_estimators': 200} |
| Ridge | -0.0721 | {'alpha': 10.0} |

**Winner:** `XGBoost` with CV r2 = **-0.0025**

---

## 5. Model Evaluation

| Metric | Value |
|---|---|
| Test RMSE | 912.3091 |
| Test R² | -0.0455 |
| CV Score (r2) | -0.0025 |

---

## 6. Final Pipeline Architecture

```
CSV Input
    └── ColumnTransformer
            ├── numeric  → SimpleImputer(median) → RobustScaler
            └── categorical → SimpleImputer(most_frequent) → OneHotEncoder
                └── XGBoost(learning_rate=0.05, max_depth=4, n_estimators=100)
                        └── Prediction
```

---

## 7. Artifacts

| File | Description |
|---|---|
| models/final_pipeline.pkl | Trained sklearn Pipeline (preprocessor + model) |
| plots/eda_correlation.png | Feature correlation heatmap |
| plots/eda_target.png | Target variable distribution |


---

## 8. Reproducibility

```python
import joblib, pandas as pd

pipeline = joblib.load("models/final_pipeline.pkl")
df = pd.read_csv("data/Insurance_1000.csv")
X = df.drop(columns=["Premium Amount"])
predictions = pipeline.predict(X)
print(predictions)
```
